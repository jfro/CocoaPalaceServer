{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {openPreferencesWindow = id; startServer = id; stopServer = id; }; 
            CLASS = MainController; 
            LANGUAGE = ObjC; 
            OUTLETS = {logView = id; statusField = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}